import 'package:flutter/material.dart';

class InProgressBooking extends StatelessWidget {
  const InProgressBooking({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
