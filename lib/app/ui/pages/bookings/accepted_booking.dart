import 'package:flutter/material.dart';

class AcceptedBooking extends StatelessWidget {
  const AcceptedBooking({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
